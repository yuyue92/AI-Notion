# 团队任务看板 · 新人使用指南

欢迎使用团队任务看板（Weekly Work Plan）！这份指南会带你了解系统里所有可以用到的功能，帮你快速上手。

---

## 一、登录账号

- 系统只允许 `@pccw.com` 邮箱注册和登录。
- 首次使用点击登录页的 **Sign Up** 标签，填写邮箱、密码（至少 6 位）、显示名称（会显示在看板上，建议填真实姓名，方便同事识别）完成注册。
- 注册成功后系统会自动帮你登录；如果提示需要邮箱验证，请查收邮件完成验证后再回到 **Log In** 标签登录。
- 账号分两种角色：
  - **Staff（普通成员）**：只能查看自己所在 Team 的看板，只能编辑自己的 Work Item。
  - **Admin（管理员）**：能查看/切换所有 Team，能编辑任何人的数据，并且能进入后台的 **Admin Settings** 页面做团队和系统配置管理。
- 如果登录后提示"你的账号尚未加入任何 Team"，请联系管理员在 **Admin Settings** 里把你加入对应的 Team。

---

## 二、主看板界面

登录后进入的就是主看板，从上到下依次是：

### 1. 顶部工具栏

- **Team 下拉框**：切换要查看的 Team（Staff 只能看到自己所在的 Team；Admin 能看到全部 Team）。
- **年份 / 周次选择**：看板是按"自然周（周一到周五）"来组织数据的，切换周次就是切换查看/编辑哪一周的工作计划。
- **Export**：把当前看板数据导出成 JSON 文件，作为备份。
- 以下几个按钮**只有 Admin 能看到**（详见第四节）：
  - **Clear Week**（清空当前周）
  - **一键导入（按成员）** 面板

### 2. 看板表格

表格是一个"成员 × 状态"的矩阵：

- 每一**行**是一个 Team 成员。
- 每一**列**是一种状态：**Pending（待办）/ Processing（进行中）/ Done（已完成）**。
- 每个格子里堆叠着该成员在这个状态下的所有 Work Item 卡片。

成员名字下面有一个 **Edit（✎）按钮**——这是你新增/编辑这个成员当周所有工作内容的唯一入口，详见第三节。

> 权限说明：Admin 能看到并点开所有成员的 Edit 按钮；Staff 只能点开自己的 Edit 按钮，看别人的名字下面不会出现 Edit 按钮（只能看，不能改）。

### 3. Work Item 卡片说明

每张卡片上会展示：

| 内容 | 说明 |
|---|---|
| 标题 | Work Item 名称 |
| 优先级标签 | Low / Medium / High / Urgent（不同颜色区分） |
| Project 标签 | 所属项目（如果填写了的话） |
| Create / Expect | 创建日期 / 预计完成日期 |
| Tasks | 这个 Work Item 下挂了几条 Task |
| 每天工时标签 | 例如 `Mon: 6`、`Fri: 8`，只显示有填工时的天，帮你一眼看出这周工时分布 |

把鼠标悬停在卡片上，会弹出一个预览框，展示这个 Work Item 下前几条 Task 的名称和描述，不用点开编辑弹框也能快速看内容。

### 4. 卡片上的两个小按钮：复制到上一周 / 下一周

每张卡片标题右侧有 `‹` `›` 两个小按钮：

- `‹` = 复制这张卡片到**上一周**
- `›` = 复制这张卡片到**下一周**

用途：这周任务没做完、下周还要接着做，不用重新录入一遍，点一下 `›` 就在下周复制出一份一模一样的（原卡片不会消失，两边可以各自独立编辑/勾选完成状态）。点击后会弹出确认框，确认信息里会显示具体目标周，避免手滑误操作。

> 这两个按钮只在你有权限编辑这个成员的数据时才会显示（自己的卡片，或者你是 Admin）。

### 5. 拖拽：直接在看板上换状态 / 转移给别人

主看板上的卡片支持**鼠标拖拽**：

- 从 Pending 列拖到 Processing 列 → 这个 Work Item 的状态直接变更。
- 拖到别的成员那一行 → 这个 Work Item 的负责人会变更（**Staff 只能在自己这一行内拖拽换状态，不能把卡片拖给别人**；Admin 可以在任意成员之间拖拽转移）。

---

## 三、编辑 Work Item（成员编辑弹框）

点击成员名字下方的 **Edit** 按钮，会打开一个大弹框，这是整个系统里**新增、编辑、删除 Work Item 和 Task 的唯一地方**。

### 1. 弹框结构

弹框内部分成三大段，跟主看板的三个状态一一对应：

- **Pending**
- **Processing**
- **Done**

每一段右上角都有一个 **＋ Work Item** 按钮，点一下就在这一段里新增一行空白的 Work Item。

### 2. 每个 Work Item 行包含的字段

| 字段 | 说明 |
|---|---|
| Item Name | Work Item 标题 |
| Project | 下拉选择所属项目（选项由 Admin 在后台维护） |
| Expect Complete Date | 预计完成日期 |
| Priority | 下拉选择优先级（选项由 Admin 在后台维护） |
| Mon ~ Fri 工时 | 每天的工时下拉选，选项是固定的一组数值（如 0、0.5、1…8，具体档位由 Admin 维护），每个下拉框下面会标出对应的具体日期（如 `Jan-10`），方便对照日历 |
| Delete Work Item | 删除这整条 Work Item（连带它下面所有 Task 一起删除，会弹确认框） |

### 3. Task（挂在 Work Item 下面）

每条 Work Item 下面有一个缩进的 **Tasks** 小区域：

- **＋ Add Task** 新增一行 Task
- 每行 Task 包含：**Task Name / Description / Remark-Blocker**，最右边一个小垃圾桶图标可以删除这条 Task
- Task 不再有"半天打卡"这种勾选，纯粹是文字描述，工时统一记录在上一层 Work Item 的 Mon~Fri 里

### 4. 拖拽换状态（在弹框内部）

弹框里每个 Work Item 行左侧有一个 **⠿** 小手柄图标，按住它可以把这一整条 Work Item **拖到另一个状态分组**（比如从 Pending 拖到 Processing），松手即完成状态切换。

> 如果 Pending 里的条目很多、需要滚动才能看到 Processing 区域：拖拽时把鼠标移到弹框内容区的上边缘或下边缘，会自动帮你滚动，不用担心"够不着"目标区域。

### 5. 保存

- 弹框内所有的增删改操作都是**本地暂存**的，右下角会提示"有未保存的修改"。
- 点击右下角 **Save & Close** 才会真正保存到服务器；保存过程中会有一个带遮罩的转圈动画，提示正在保存，请耐心等待，不要重复点击。
- 如果不想保存这次修改，直接点右上角的 **×** 关闭，弹框内做的所有改动都会被丢弃，主看板不会有任何变化。
- 新增了一行但什么都没填就直接保存，这种空白行会被自动跳过、不会产生垃圾数据。

---

## 四、管理员专属功能

以下功能只有 **Admin** 角色能看到和使用。

### 1. Clear Week（清空当前周）

一键清空当前 Team、当前周的**所有**成员的所有 Work Item，用于重置某一周的数据。操作前会有二次确认，请谨慎使用，此操作不可撤销。

### 2. 一键导入（按成员）

在顶部工具栏可以选择：某个成员 + 来源年份/周次，把该成员**某一周的全部数据**一次性复制到当前周。如果目标周已经有该成员的数据，会先提示"是否覆盖"再继续。

> 这个功能和卡片上的"复制到上/下周"是两回事：前者是"整周批量搬"，后者是"单条卡片精确续到下一周"，按需选择合适的方式。

### 3. 成员排序

在看板成员列表里，Admin 可以点成员名字旁边的 **↑** 按钮，调整成员在看板里的显示顺序。

### 4. Admin Settings（管理后台）

点顶部导航的 **Admin Settings** 按钮进入后台管理页，里面包括：

#### （1）Registered Users（已注册用户）
- 查看所有已注册账号，给账号分配到某个 Team、或从 Team 里移除。
- **禁用账号**：会清空该账号名下的所有 Work Item / Task 数据，且账号无法再登录，此操作不可恢复，请谨慎操作。
- **启用账号**：恢复被禁用的账号，使其可以重新登录（不会找回被清空的历史数据）。

#### （2）Teams（团队管理）
- 新增 Team、给 Team 改名、删除 Team。
- 删除前系统会检查：如果这个 Team 底下还有成员，会拦下并提示先移除所有成员再删除。

#### （3）Team Members Overview（团队成员总览）
- 按 Team 分组，一览各个 Team 目前都有哪些成员，方便核对分配情况。

#### （4）Projects / Priority / Hour Options（下拉选项管理）
这三块管理的是编辑 Work Item 时用到的下拉选项，都在同一套管理组件里操作（新增/改名/删除）：
- **Projects**：Work Item 编辑弹框里 Project 下拉框的可选项。
- **Priority**：优先级下拉框的可选项。
- **Hour Options**：Mon~Fri 工时下拉框的可选档位（比如 0、0.5、1…）。

删除某个选项时，如果已经有 Work Item 在用这个值，系统会提示"有 N 条数据正在使用该选项"，删除后已有数据不会被清空或改变，只是这个值以后不会再出现在下拉框里可供选择。

---

## 五、常见问题

**Q：为什么我看不到别的同事的 Edit 按钮？**
A：Staff 角色只能编辑自己的数据，看别人只能看不能改，这是正常权限设计。有编辑需求请联系 Admin。

**Q：卡片上 Mon~Fri 的工时标签怎么一天都没有？**
A：说明这条 Work Item 还没有在编辑弹框里填任何工时，工时标签只展示大于 0 的那几天，都是 0 就不会显示。

**Q：编辑弹框里改了半天，不小心点错关掉了怎么办？**
A：只要没点 **Save & Close**，所有改动都不会真正保存，重新点开 Edit 按钮，看到的还是修改前的数据。

**Q：切换 Team 或切换周的时候页面转圈，是卡住了吗？**
A：不是卡住，是正在从服务器拉取数据，稍等即可，转圈结束数据就会刷新出来。

**Q：一键导入和卡片上的"复制到下一周"应该用哪个？**
A：如果是"这一条任务还没做完、想续到下周"，用卡片上的 `›` 按钮就够了；如果是"这个人这一整周的安排都要照抄到另一周"，用顶部工具栏的"一键导入（按成员）"更省事（仅 Admin 可用）。

---

如有其他使用问题，请联系团队管理员（Admin）协助处理。
