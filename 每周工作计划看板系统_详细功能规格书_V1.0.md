# 每周工作计划看板系统功能规格书

> 文档名称：Weekly Work Plan Team Board 功能规格书  
> 文档版本：V1.0  
> 编制日期：2026-07-14  
> 系统类型：部门内部周工作计划与进度管理系统  
> 技术栈：Vue 3、Vue Router、Supabase Auth、PostgreSQL、Row Level Security  
> 适用对象：产品负责人、项目经理、前端开发、数据库开发、测试人员、系统管理员

---

## 1. 文档目的

本文档用于完整描述“每周工作计划看板系统”的业务定位、用户角色、功能范围、核心流程、业务规则、权限边界、数据对象、页面行为及验收标准。

本文档可用于：

- 项目需求确认；
- 功能迭代规划；
- 前后端开发对齐；
- 数据库与权限设计；
- 测试用例编写；
- 项目部署、交接和长期维护；
- 后续版本变更记录。

---

## 2. 系统概述

### 2.1 系统名称

**每周工作计划看板系统**

英文名称：**Weekly Work Plan Team Board**

### 2.2 系统定位

系统面向部门或团队内部使用，以“团队—成员—工作周”为核心组织方式，对每位成员一周内的工作事项进行统一维护和可视化展示。

系统通过三种状态展示事项进展：

- Pending：待处理；
- Processing：处理中；
- Done：已完成。

管理员可查看和维护全部团队数据，普通员工只能查看和操作其权限范围内的数据。系统使用 Supabase Auth 进行身份认证，并通过数据库 RLS 与 RPC 约束数据访问权限。

### 2.3 主要业务目标

1. 统一记录团队成员每周工作计划；
2. 直观展示事项执行状态；
3. 将工作事项拆分为具体任务；
4. 记录周一至周五的投入工时；
5. 支持跨状态、跨成员的事项流转；
6. 支持复制上一周计划，减少重复录入；
7. 支持管理员统一维护团队、人员及配置项；
8. 支持数据导出，用于留档和备份；
9. 支持空闲会话自动退出，降低共享设备上的账号风险。

---

## 3. 系统范围

### 3.1 当前包含

- 用户登录、注册、忘记密码、重置密码；
- 用户资料读取和账号禁用；
- 登录状态恢复；
- 会话空闲超时和绝对时长限制；
- 团队、年份、工作周选择；
- 上一周、下一周切换；
- 周工作看板；
- Work Item 和 Task 管理；
- 每日工时管理；
- 主看板拖拽和状态流转；
- 成员级统一编辑；
- 成员排序；
- 单事项复制到相邻周；
- 按成员复制指定来源周；
- 清空当前周；
- JSON 导出；
- Project、Priority、Hour 配置；
- 管理员页面；
- 路由权限控制；
- 加载状态和操作反馈。

### 3.2 当前未完整包含

- JSON 导入恢复；
- 邮件通知；
- 审批流程；
- 评论和讨论；
- 文件附件；
- 操作审计日志；
- 数据版本历史；
- 自定义状态流；
- 甘特图；
- 统计报表；
- KPI 或绩效评分；
- 离线编辑；
- 实时多人冲突解决。

---

## 4. 用户角色与权限

### 4.1 角色定义

| 角色 | 说明 |
|---|---|
| Staff | 普通员工，只能访问被分配的团队和其权限范围内的数据 |
| Admin | 管理员，可访问全部团队，并执行管理类操作 |
| Disabled User | 被禁用账号，不允许继续使用系统 |
| Anonymous | 未登录用户，只能访问登录和重置密码页面 |

### 4.2 权限矩阵

| 功能 | Anonymous | Staff | Admin |
|---|---:|---:|---:|
| 访问登录页 | 是 | 否，自动跳转看板 | 否，自动跳转看板 |
| 注册 | 是 | 否 | 否 |
| 忘记或重置密码 | 是 | 是 | 是 |
| 查看工作看板 | 否 | 是 | 是 |
| 查看已分配 Team | 否 | 是 | 是 |
| 查看全部 Team | 否 | 否 | 是 |
| 编辑本人事项 | 否 | 由 RLS 决定 | 是 |
| 编辑其他成员事项 | 否 | 默认否 | 是 |
| 同成员跨状态拖拽 | 否 | 由 RLS 决定 | 是 |
| 跨成员拖拽 | 否 | 默认否 | 是 |
| 调整成员排序 | 否 | 否 | 是 |
| 清空当前周 | 否 | 否 | 是 |
| 一键导入成员周数据 | 否 | 否 | 是 |
| 进入管理员页面 | 否 | 否 | 是 |
| 管理配置项 | 否 | 否 | 是 |
| 导出 JSON | 否 | 可配置 | 是 |
| 手动退出 | 否 | 是 | 是 |

> 前端按钮隐藏不能替代数据库权限控制。最终权限应由 Supabase RLS、数据库约束及 RPC 内部角色校验共同保证。

---

## 5. 系统架构概览

### 5.1 前端模块职责

| 模块 | 职责 |
|---|---|
| `main.js` | 初始化 Vue、注册路由、恢复认证状态后挂载应用 |
| `App.vue` | 全局布局、会话过期弹框、统一退出跳转 |
| `router/index.js` | 路由定义、登录校验、管理员校验 |
| `useAuth.js` | 登录、注册、密码重置、Session、Profile |
| `useBoardStore.js` | Team、Week、Board、Work Item、Task、导入、导出 |
| `date.js` | 日期、周范围、年份处理 |
| `model.js` | 前端模型与数据库字段转换 |
| `constants/index.js` | 状态、工作日、邮箱域等常量 |
| `main.css` | 登录页、看板、弹框、管理页样式 |

### 5.2 后端服务

系统使用 Supabase 提供：

- Auth 用户认证；
- PostgreSQL 数据库；
- Row Level Security；
- RPC 数据库函数；
- Session 与 Refresh Token；
- 用户资料；
- Team 与成员关系；
- Work Item 与 Task 存储。

### 5.3 启动流程

1. 浏览器加载前端应用；
2. 创建 Vue 应用和路由；
3. 调用认证模块 `init()`；
4. 恢复 Supabase Session；
5. 读取用户 Profile；
6. 判断账号是否禁用；
7. 建立 Session 计时器；
8. 认证初始化完成后挂载 Vue；
9. 路由守卫决定目标页面。

---

## 6. 路由规格

| 路径 | 路由名称 | 页面 | 需要登录 | 需要管理员 |
|---|---|---|---:|---:|
| `/login` | Login | 登录、注册、忘记密码 | 否 | 否 |
| `/reset-password` | ResetPassword | 设置新密码 | 否 | 否 |
| `/` | Board | 周工作计划看板 | 是 | 否 |
| `/admin` | Admin | 管理员设置 | 是 | 是 |
| 其他路径 | - | 重定向 `/` | 按目标页 | 按目标页 |

路由规则：

- 未登录访问 `/` 或 `/admin`：跳转 `/login`；
- 已登录访问 `/login`：跳转 `/`；
- Staff 访问 `/admin`：跳转 `/`；
- 未定义路由：重定向 `/`，再由守卫判断权限。

---

## 7. 用户认证功能

### 7.1 登录

输入字段：

| 字段 | 必填 | 说明 |
|---|---:|---|
| Email | 是 | 用户邮箱 |
| Password | 是 | 用户密码 |

处理流程：

1. 调用 Supabase 密码登录；
2. 登录失败时显示错误；
3. 登录成功后读取 `profiles`；
4. 检查 `is_disabled`；
5. 被禁用时立即退出；
6. 正常时建立应用 Session；
7. 记录会话起始时间和最后活动时间；
8. 跳转看板。

### 7.2 注册

输入字段：

| 字段 | 必填 | 说明 |
|---|---:|---|
| Display Name | 是 | 系统显示名称 |
| Email | 是 | 仅允许企业邮箱域 |
| Password | 是 | 密码 |
| Confirm Password | 是 | 确认密码 |

当前邮箱域：

```text
@pccw.com
```

前端应先对邮箱执行 `trim()` 和 `toLowerCase()`，再检查域名。正式环境还必须在服务端再次校验。

### 7.3 忘记密码

流程：

1. 用户点击 Forgot Password；
2. 输入邮箱；
3. 调用重置密码邮件接口；
4. 邮件链接跳转 `/reset-password`；
5. Supabase 建立 Recovery Session；
6. 用户输入新密码；
7. 调用更新密码接口；
8. 成功后进入登录页或看板。

### 7.4 账号禁用

当 `profiles.is_disabled = true` 时，系统必须：

- 不建立业务登录状态；
- 清空 Session 和 Profile；
- 清除定时器与监听器；
- 跳转 Login；
- 显示账号禁用提示。

---

## 8. 会话管理功能

### 8.1 当前策略

| 策略 | 当前值 |
|---|---:|
| 空闲超时 | 10 分钟 |
| 提前提醒 | 2 分钟 |
| 绝对最长会话 | 8 小时 |
| 活动写入节流 | 15 秒 |

### 8.2 用户活动

以下事件视为活动：

- click；
- keydown；
- mousemove；
- scroll；
- touchstart；
- 页面重新变为 visible。

### 8.3 空闲超时流程

1. 登录后记录最后活动时间；
2. 有效活动刷新空闲到期时间；
3. 到达提醒点显示弹框；
4. 提醒弹出后普通鼠标活动不自动续期；
5. 用户必须点击 Continue Session；
6. 倒计时归零后自动退出；
7. 清除本地状态；
8. 跳转 Login。

### 8.4 绝对时长

登录满 8 小时后，无论是否持续操作都必须重新登录。刷新页面、Token Refresh、路由切换均不得重置该时间。

### 8.5 会话提醒弹框

包含：

- 图标；
- 标题；
- 说明文字；
- 倒计时；
- Continue Session；
- Sign Out。

### 8.6 自动退出要求

倒计时到 `00:00` 后必须：

1. 停止倒计时；
2. 防止重复退出；
3. 调用认证退出；
4. 即使远端退出失败，也清除本地状态；
5. 自动跳转 Login。

---

## 9. Team 与成员功能

### 9.1 Team 范围

Admin：读取全部 Team。  
Staff：只读取 `team_users` 中与本人关联的 Team。

没有任何 Team 时，显示“尚未分配 Team，请联系管理员”，不能展示空白看板。

### 9.2 Team 切换

切换 Team 后应：

1. 更新 `teamId` 和 Team 名称；
2. 清空旧成员和旧看板；
3. 关闭正在编辑的弹框；
4. 加载新 Team 成员；
5. 重新生成当前年份周列表；
6. 确保当前周有效；
7. 加载新 Team 当前周数据；
8. 更新一键导入默认值。

### 9.3 成员结构

| 字段 | 说明 |
|---|---|
| userId | 用户 ID |
| displayName | 显示名称 |
| role | 角色 |
| sortOrder | 看板排序 |

### 9.4 成员排序

管理员可将成员与上一位交换顺序。第一位成员不可上移。操作失败时保持原顺序。

---

## 10. 年份与工作周功能

### 10.1 年份范围

允许范围：`2000—2100`。无效年份回退为当前年份。

### 10.2 工作周定义

工作周从周一到周五，周末不纳入工时。

### 10.3 周对象

```json
{
  "key": "2026-W29",
  "weekNo": 29,
  "startDate": "2026-07-13",
  "endDate": "2026-07-17",
  "days": [
    "2026-07-13",
    "2026-07-14",
    "2026-07-15",
    "2026-07-16",
    "2026-07-17"
  ],
  "label": "week 29 2026-07-13 ~ 2026-07-17"
}
```

### 10.4 默认周

- 当前年份：默认包含今天的周；
- 非当前年份：默认第一周。

### 10.5 周切换

支持上一周、下一周。达到边界时对应按钮禁用。

### 10.6 跨年规则

建议数据库用 `week_start_date` 作为唯一业务标识，避免同一自然周跨年后出现两个不同 `year + week_key`。

---

## 11. 看板页面功能

### 11.1 页面头部

应显示：

- 企业标识；
- 系统名称；
- 副标题；
- 当前用户名；
- 当前角色；
- Admin Settings；
- Sign Out。

Admin Settings 仅管理员可见。

### 11.2 工具栏

包含：

- Team；
- Year；
- Week；
- 上一周、下一周；
- Start Date；
- End Date；
- Export JSON；
- Clear Current Week；
- 一键导入面板。

Start Date 和 End Date 只读。Clear Current Week 和一键导入仅 Admin 可见。

### 11.3 看板结构

每位成员一行，每行四列：

| 列 | 内容 |
|---|---|
| Member | 成员名称及编辑入口 |
| Pending | 待处理事项 |
| Processing | 处理中事项 |
| Done | 已完成事项 |

### 11.4 空状态

无事项时分别显示：

- No Pending items；
- No Processing items；
- No Done items。

---

## 12. Work Item 功能

### 12.1 数据结构

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | UUID | 自动 | 主键 |
| team_id | UUID | 是 | 所属 Team |
| owner_id | UUID | 是 | 所属成员 |
| year | Integer | 是 | 年份 |
| week_key | String | 是 | 工作周 |
| status | Enum | 是 | pending / processing / done |
| work_item | String | 建议必填 | 标题 |
| project_name | String | 否 | 项目 |
| ref_id | String | 否 | 参考编号 |
| priority | String | 是 | 优先级 |
| expect_complete_date | Date | 否 | 预计完成日期 |
| create_date | Date | 否 | 创建日期 |
| mon_hours～fri_hours | Number | 是 | 每日工时 |
| updated_at | Timestamp | 建议 | 更新时间 |
| version | Integer | 建议 | 并发版本 |

### 12.2 新建默认值

| 字段 | 默认值 |
|---|---|
| owner_id | 当前成员 |
| status | 当前状态区 |
| priority | Medium |
| create_date | 当天 |
| 每日工时 | 0 |
| tasks | 空数组 |

### 12.3 可编辑字段

- 标题；
- Project；
- Ref ID；
- Priority；
- Expected Complete Date；
- 周一至周五工时；
- Tasks；
- Status。

普通编辑不应修改 Team、Owner、Year、Week、Create Date。

### 12.4 删除

删除前二次确认，提示中包含事项标题。删除 Work Item 时 Tasks 应同步删除，建议数据库使用 `ON DELETE CASCADE`。

### 12.5 卡片展示

应显示：

- 标题；
- Project；
- Ref ID；
- Priority；
- Create Date；
- Expected Date；
- Task 数量；
- 每日工时和周总工时；
- 上一周、下一周复制按钮。

### 12.6 悬停预览

预览可显示完整标题、元信息、Tasks、描述、Blocker、工时，并自动防止超出屏幕。

---

## 13. Task 功能

### 13.1 数据结构

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | UUID | 自动 | 主键 |
| work_item_id | UUID | 是 | 所属事项 |
| task_name | String | 建议必填 | Task 名称 |
| description | Text | 否 | 描述 |
| remark_blocker | Text | 否 | 备注或阻塞事项 |
| sort_order | Integer | 是 | 排序 |

### 13.2 有效性

以下任一字段有内容时视为有效：

- task_name；
- description；
- remark_blocker。

完全空白 Task 不写入数据库。

### 13.3 排序

保存时按数组顺序生成连续 `sort_order`。

---

## 14. 工时功能

### 14.1 工作日

- Monday；
- Tuesday；
- Wednesday；
- Thursday；
- Friday。

### 14.2 工时选项来源

来自 `list_options`，其中 `list_type = hour`。

### 14.3 周总工时

```text
Monday + Tuesday + Wednesday + Thursday + Friday
```

### 14.4 校验

建议前后端同时限制：

- 必须为数字；
- 不得小于 0；
- 单日不得超过业务上限；
- 小数精度统一；
- 数据库添加 CHECK；
- 禁止 NaN、Infinity 和非法字符串。

---

## 15. 成员级编辑弹框

### 15.1 功能目标

在一个弹框中一次性编辑某成员当前周全部 Pending、Processing、Done 数据。

### 15.2 支持操作

- 新增 Work Item；
- 编辑 Work Item；
- 删除 Work Item；
- 新增 Task；
- 删除 Task；
- 三状态之间拖拽；
- 设置工时；
- 保存并关闭；
- 放弃修改。

### 15.3 草稿模式

打开时克隆当前数据，修改只作用于本地草稿，不自动落库。点击 Save & Close 后统一提交。

### 15.4 保存要求

1. 遍历三种状态；
2. 校正每个事项状态；
3. 新事项新增；
4. 旧事项更新；
5. 保存有效 Task；
6. 删除标记事项和 Task；
7. 全部成功后重新加载；
8. 显示 Saved；
9. 关闭弹框。

### 15.5 事务要求

正式版本应将成员整周新增、更新、删除合并为一个数据库 RPC，在单个 PostgreSQL 事务中执行。任何一步失败时整体回滚。

### 15.6 未保存保护

发生修改后，关闭弹框、点击遮罩、按 Esc、切换 Team/Year/Week、路由跳转或刷新页面时应提示用户。

---

## 16. 拖拽功能

### 16.1 弹框内拖拽

同一成员三种状态之间拖拽，只修改草稿，保存后落库。

### 16.2 主看板拖拽

支持：

- 同成员跨状态；
- Admin 跨成员；
- 其他符合 RLS 的操作。

### 16.3 校验

必须校验：

- itemId 存在；
- targetStatus 合法；
- targetOwnerId 属于当前 Team；
- 当前用户有修改权限；
- 跨成员操作符合角色权限；
- 源和目标相同时不执行。

### 16.4 失败处理

失败时显示错误。RLS 拒绝后重新加载看板，恢复数据库真实状态。

---

## 17. 单事项复制到相邻周

### 17.1 功能

复制某个 Work Item 到上一周或下一周，原事项保留。

### 17.2 复制内容

- Work Item 字段；
- Project；
- Ref ID；
- Priority；
- Status；
- 工时；
- Tasks。

### 17.3 防重复

复制进行中禁用该事项复制按钮，同一 itemId 不允许重复提交。

### 17.4 日期规则

建议：

| 字段 | 处理 |
|---|---|
| expect_complete_date | 随目标周平移 7 天 |
| create_date | 保留原日期或改为复制日，需确认 |
| 工时 | 原样复制 |
| Task 日期 | 将来若存在，同步平移 |

---

## 18. 一键导入

### 18.1 功能

Admin 将某成员指定来源周的数据复制到当前目标周。

### 18.2 输入项

- Member；
- Source Year；
- Source Week；
- 当前 Team；
- 当前 Target Year；
- 当前 Target Week。

### 18.3 默认值

- Member：当前 Team 第一位成员；
- Source Year/Week：当前目标周的上一自然周；
- Target：当前看板 Team、Year、Week。

### 18.4 校验

以下情况不执行：

- 未选择成员；
- 未选择来源周；
- 来源和目标完全相同；
- Board 正在加载；
- 已有导入任务进行中。

### 18.5 目标已有数据

统计该成员目标周事项数量，显示二次确认，并明确现有数据将被清除后替换。

### 18.6 日期平移

通过：

```text
目标周开始日期 - 来源周开始日期
```

计算 `p_shift_days`。

### 18.7 权限

按钮仅 Admin 可见，RPC 内部也必须再次校验 Admin。

---

## 19. 清空当前周

### 19.1 范围

删除条件：

```text
team_id = 当前 Team
year = 当前 Year
week_key = 当前 Week
```

### 19.2 二次确认

提示必须包含 Team、Week 和不可撤销说明。

### 19.3 结果

成功后重新加载看板，显示 Current week cleared。Tasks 应级联删除。

---

## 20. 配置项管理

统一存储在 `list_options`。

| list_type | 用途 |
|---|---|
| project | Project 下拉 |
| priority | Priority 下拉 |
| hour | Hour 下拉 |

加载顺序：`list_type → sort_order → name`。

配置加载失败时，看板主体仍可加载，但相关下拉应提示异常并避免覆盖原值。

---

## 21. JSON 导出

### 21.1 导出内容

```json
{
  "version": 4,
  "app": "Weekly Work Plan Team Board",
  "exportedAt": "2026-07-14T08:00:00.000Z",
  "team": "Customer_Service_Process",
  "year": 2026,
  "weekKey": "2026-W29",
  "boardData": {},
  "members": []
}
```

### 21.2 文件名

```text
weekly_board_YYYYMMDD_HHMMSS.json
```

当前只支持导出，不支持导入恢复，因此该功能属于“数据导出”，不是完整数据库备份。

---

## 22. 加载和反馈

### 22.1 全局加载

适用于：

- 认证初始化；
- 页面首次进入；
- Team 切换；
- Board 加载；
- 成员保存；
- 导入；
- 配置加载。

### 22.2 局部加载

适用于：

- Save & Close；
- Import This Member；
- 复制事项；
- 清空当前周；
- 成员排序。

### 22.3 Toast

统一提示：

- 保存成功或失败；
- 导入成功或失败；
- 拖拽成功；
- 权限不足；
- 导出成功；
- Team 或配置加载失败。

---

## 23. 核心数据对象

### 23.1 Profile

```json
{
  "id": "uuid",
  "display_name": "Yu Yue",
  "role": "admin",
  "is_disabled": false
}
```

### 23.2 Team

```json
{
  "id": "uuid",
  "name": "Customer_Service_Process"
}
```

### 23.3 Team User

```json
{
  "team_id": "uuid",
  "user_id": "uuid",
  "sort_order": 10
}
```

### 23.4 Work Item

```json
{
  "id": "uuid",
  "team_id": "uuid",
  "owner_id": "uuid",
  "year": 2026,
  "week_key": "2026-W29",
  "status": "processing",
  "work_item": "Data Extraction",
  "project_name": "Project A",
  "ref_id": "REF-001",
  "priority": "Medium",
  "expect_complete_date": "2026-07-17",
  "create_date": "2026-07-10",
  "mon_hours": 2,
  "tue_hours": 4,
  "wed_hours": 1,
  "thu_hours": 0,
  "fri_hours": 0
}
```

### 23.5 Task

```json
{
  "id": "uuid",
  "work_item_id": "uuid",
  "task_name": "Prepare source data",
  "description": "Extract and validate source files",
  "remark_blocker": "",
  "sort_order": 0
}
```

---

## 24. 数据库约束建议

### Work Item

建议添加：

```sql
CHECK (status IN ('pending', 'processing', 'done'))
```

每日工时添加范围检查，Tasks 外键设置 `ON DELETE CASCADE`。

建议索引：

- `team_id, year, week_key`；
- `owner_id, year, week_key`；
- `status`；
- `updated_at`。

### Team User

唯一约束：

```text
team_id + user_id
```

### List Option

唯一约束：

```text
list_type + lower(name)
```

---

## 25. RLS 权限规格

### Profiles

- 用户可读取本人 Profile；
- 可读取同 Team 成员必要公开字段；
- 不可自行修改 role 和 is_disabled；
- Admin 可管理用户资料和禁用状态。

### Teams 和 Team Users

- Staff 只能读取本人所属 Team；
- Admin 可管理 Team 和成员关系；
- Staff 不可任意新增或删除成员关系。

### Work Items

- Staff 只可修改本人事项；
- Staff 默认不可把事项转给其他成员；
- Admin 可管理全部 Team 事项；
- 数据库必须校验目标成员属于当前 Team。

### Tasks

Task 权限继承所属 Work Item 权限，不能只判断 Task 自身字段。

### RPC

所有 `SECURITY DEFINER` RPC 必须检查：

- `auth.uid()`；
- 当前用户角色；
- Team 归属；
- Owner 合法性；
- 来源与目标周；
- 是否允许删除和替换。

---

## 26. 并发和一致性要求

### 26.1 Board 加载

必须防止旧请求覆盖新请求：

- 每次加载生成 requestId；
- 捕获 Team、Year、Week 快照；
- 返回时只接收最新请求；
- 旧请求结果直接丢弃。

### 26.2 Tasks 加载失败

Work Items 成功但 Tasks 失败时：

- 不把 Tasks 当空数组；
- 不允许继续编辑；
- 不覆盖现有 Board；
- 提示重新加载。

### 26.3 成员保存事务

成员整周保存必须使用单个事务。任何一步失败，新增、更新、删除都整体回滚。

### 26.4 乐观锁

建议增加 `updated_at` 或 `version`。保存时若版本不一致，提示：

```text
该事项已被其他用户修改，请刷新后重新编辑。
```

---

## 27. 安全要求

- 前端权限不是安全边界；
- 邮箱域必须服务端校验；
- Admin 操作必须由 RLS/RPC 再次校验；
- localStorage 时间戳只能作为 UX，不是安全控制；
- 不在 localStorage 保存业务数据；
- 不输出 Access Token；
- Production 不暴露 SQL 错误；
- 全站 HTTPS；
- Supabase service role key 禁止进入前端代码。

---

## 28. 性能要求

| 操作 | 目标响应时间 |
|---|---:|
| 登录 | 3 秒内 |
| 首次 Board 加载 | 3 秒内 |
| Team 切换 | 3 秒内 |
| Week 切换 | 2 秒内 |
| 保存成员数据 | 5 秒内 |
| 拖拽更新 | 2 秒内 |
| 一键导入 | 10 秒内 |
| JSON 导出 | 2 秒内 |

建议通过索引、限定查询范围、减少重复加载配置和批量 RPC 优化性能。

---

## 29. 验收标准

### 29.1 登录与权限

- [ ] 未登录访问首页自动跳转 Login；
- [ ] 已登录访问 Login 自动跳转 Board；
- [ ] Staff 无法进入 Admin；
- [ ] Admin 可以进入 Admin；
- [ ] Disabled User 无法进入系统；
- [ ] 非企业邮箱无法注册；
- [ ] 服务端也限制邮箱域；
- [ ] Profile 加载失败时不能错误进入系统。

### 29.2 会话

- [ ] 空闲到提醒点显示弹框；
- [ ] 倒计时每秒正确减少；
- [ ] Continue Session 可续期；
- [ ] 提醒弹出后鼠标移动不自动续期；
- [ ] 归零自动退出；
- [ ] 退出后自动跳转 Login；
- [ ] 重复退出不破坏状态；
- [ ] 绝对时长到期必须退出；
- [ ] 刷新不重置绝对时长。

### 29.3 Team 和 Week

- [ ] Admin 可查看全部 Team；
- [ ] Staff 只看到所属 Team；
- [ ] 无 Team 用户看到明确提示；
- [ ] 切换 Team 后不残留旧数据；
- [ ] Year 和 Week 切换正确；
- [ ] 上下周边界正确；
- [ ] 跨年周不重复；
- [ ] Start/End Date 正确。

### 29.4 Board 和编辑

- [ ] 每位成员显示一行；
- [ ] 三种状态数据正确；
- [ ] 空状态有提示；
- [ ] 卡片元信息和 Tasks 数量正确；
- [ ] 工时计算正确；
- [ ] 卡片顺序稳定；
- [ ] 可新增、编辑、删除 Work Item；
- [ ] 可新增、删除 Task；
- [ ] 可在三状态间拖拽；
- [ ] 保存失败不产生部分结果；
- [ ] 未保存关闭有确认；
- [ ] 并发冲突有提示。

### 29.5 复制、导入和清空

- [ ] 单事项可复制到前后周；
- [ ] 原事项保留；
- [ ] Tasks 一并复制；
- [ ] 日期按规则平移；
- [ ] 重复点击被阻止；
- [ ] 一键导入成功；
- [ ] 来源和目标相同时拒绝；
- [ ] 目标已有数据时二次确认；
- [ ] 替换过程使用事务；
- [ ] Staff 无法调用 Admin RPC；
- [ ] 清空只影响当前 Team 和 Week；
- [ ] Tasks 同步删除。

### 29.6 JSON 导出

- [ ] 文件正常下载；
- [ ] 文件名包含时间戳；
- [ ] 导出内容与当前 Board 一致；
- [ ] version 字段存在。

---

## 30. 版本规划

### V1.0 当前基础版本

- 登录注册；
- Team 周看板；
- Work Item、Task、工时；
- 拖拽；
- 一键导入；
- JSON 导出；
- Admin 设置；
- Session 超时。

### V1.1 稳定性版本

- 修复异步请求覆盖；
- Tasks 加载失败保护；
- 成员整周事务保存；
- 乐观锁；
- 跨年周修复；
- 多标签页 Session 同步；
- 登出清理 Board Store；
- 统一错误处理。

### V1.2 体验版本

- 未保存离开提醒；
- 稳定排序；
- 搜索和筛选；
- 移动端适配；
- JSON 导入；
- 更友好的批量编辑。

### V2.0 管理和统计版本

- 操作审计；
- 周报统计；
- 工时汇总；
- 项目报表；
- 成员负载分析；
- Excel 导出；
- 管理员仪表盘；
- 版本历史和恢复；
- 实时协同和冲突提示。

---

## 31. 已知风险

| 风险 | 影响 | 优先级 |
|---|---|---|
| 旧请求覆盖新周数据 | 页面周和实际数据不一致 | P0 |
| Tasks 请求失败仍允许编辑 | 可能误删 Tasks | P0 |
| 成员保存非整体事务 | 产生部分成功数据 | P0 |
| 跨年周重复 | 重复数据、复制错误 | P1 |
| 多人同时编辑 | 后保存覆盖前者 | P1 |
| 前端邮箱限制可绕过 | 非企业邮箱注册 | P1 |
| 多标签页会话不同步 | 活跃页面被意外退出 | P1 |
| Profile 失败仍继续登录 | 禁用检查和角色读取异常 | P1 |
| 默认 Priority 不一致 | 数据异常变化 | P2 |
| 无稳定排序字段 | 刷新后顺序变化 | P2 |
| 无未保存提示 | 修改丢失 | P2 |
| JSON 只有导出 | 无法恢复 | P2 |

---

## 32. 术语表

| 术语 | 说明 |
|---|---|
| Team | 团队或部门 |
| Member | Team 内成员 |
| Work Item | 工作事项 |
| Task | Work Item 下的子任务 |
| Pending | 尚未开始 |
| Processing | 正在处理 |
| Done | 已完成 |
| RLS | PostgreSQL Row Level Security |
| RPC | Supabase 数据库函数调用 |
| Session | 用户登录会话 |
| Recovery Session | 密码重置临时会话 |
| Week Key | 工作周标识 |
| Owner | Work Item 所属成员 |
| Draft | 未保存草稿 |
| Toast | 页面短提示 |

---

## 33. 文档维护记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0 | 2026-07-14 | 根据当前项目代码整理首版详细功能规格书 |

---

## 34. 结论

当前系统已经具备部门内部周工作计划管理的主要业务能力，功能范围较完整，前端模块划分基本合理。

正式长期使用前，建议优先完成：

1. Board 请求竞态保护；
2. Tasks 加载失败保护；
3. 成员整周事务保存；
4. 跨年周唯一标识修复；
5. 数据并发版本控制；
6. RLS 与 RPC 权限复核；
7. 多标签页 Session 同步；
8. 数据库 Migration 标准化。

完成上述内容后，系统可从内部 MVP 提升为更稳定、可持续维护的部门级工作计划管理系统。
