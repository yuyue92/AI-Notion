好，给你准备几个不同场景的案例，可以直接收藏套用。我按"新建项目""现有项目迭代""Bug修复/重构""纯方案讨论（不写代码）"四类来给，覆盖你日常大概率会遇到的情况。

---

## 案例1：新建一个完整模块/系统

```
【Step1 - 方案确认】
需求：实现一个 xxx 管理系统，功能包括：
1. xxx
2. xxx
3. xxx

技术选型：Vue3 + TypeScript + Pinia + Vue Router
约束：
- 不引入 UI 组件库，纯手写组件
- 需要支持移动端响应式
- 接口暂用 mock 数据，先不接真实后端

请先给出方案，不要写代码，必须包含：
1. 核心数据结构（主要实体的字段、类型、相互关系）
2. 你认为有歧义、需要我确认的点（明确列出，不要省略）
3. 模块/文件拆分方式（页面、组件、store、utils 大致怎么分）
4. 关键技术决策（比如表单校验用什么方式、列表是否要分页/虚拟滚动）

【确认后，Step2 - 分步实现】
理解一致后：
1. 先给出工程目录结构（仅目录树，不要代码），我确认后再继续
2. 确认后按文件逐个给出代码，每完成一个文件暂停，不要一次性输出全部
3. 全部完成后，对照 Step1 的功能清单逐条核对是否都已实现，明确列出核对结果，不要只说"已完成"
```

---

## 案例2：在现有项目基础上加功能（最常用，建议重点收藏）

```
【背景】
这是在现有项目基础上修改，不是新建项目。
项目技术栈：<Vue3/React + 具体库>
现有相关代码：（贴出涉及的文件，或描述现有结构）

【需求】
需要新增/修改：xxx功能，具体表现为：
1. xxx
2. xxx

约束：
- 不能改动现有 xxx 模块的对外接口/props
- 命名风格、代码风格需与现有代码保持一致

【Step1 - 方案确认】
请先给出方案，不要写代码：
1. 这次改动会涉及哪些文件（新增 or 修改，逐一列出）
2. 是否会影响现有功能，影响范围是什么
3. 有歧义需要我确认的点

【确认后，Step2 - 实现】
只输出改动的文件，每个文件开头注明：
- "【新增文件】路径：xxx"
- "【修改文件】原路径：xxx，改动说明：xxx"
不要重复输出未改动的文件。

完成后对照需求逐条核对实现情况，列出结果。
```

---

## 案例3：Bug 修复 / 代码重构

```
【背景】
技术栈：<xxx>
问题描述：<现象，比如"点击保存按钮后，列表数据没有刷新，但接口返回成功">
相关代码：（贴出报错堆栈 / 相关文件片段）

复现步骤：
1. xxx
2. xxx
期望结果：xxx
实际结果：xxx

【Step1 - 定位分析】
先不要直接给修复代码，请：
1. 分析可能的原因，按可能性从高到低列出（至少2-3种可能）
2. 针对每种原因，说明可以怎么验证（比如加什么日志、看什么状态）
3. 如果信息不够判断，明确说出还需要我提供什么信息

【确认原因后，Step2 - 修复】
1. 给出修复方案和改动范围
2. 给出具体代码改动（diff 形式更好：原代码 → 改后代码）
3. 说明这个改动是否可能引入新的副作用，需要注意什么
```

---

## 案例4：纯方案讨论（暂不写代码，技术选型/架构决策阶段）

```
【背景】
我正在规划 xxx 项目/模块，目前处于技术选型阶段，先不需要代码。

需求要点：
1. xxx
2. xxx

候选方案：<比如 A方案 vs B方案，或者"还没想好方案，请你给建议">

请帮我分析，按以下结构输出：
1. 每个方案的优缺点（结合我的具体场景，不要泛泛而谈框架优缺点）
2. 在我的约束下（<比如团队规模、性能要求、维护成本>），你会推荐哪个，理由是什么
3. 这个决策未来可能带来的隐患或迁移成本

不需要给代码，给决策依据即可。
```

---

### 几个通用收藏建议

- **案例2 是日常使用频率最高的**，因为大部分工作是在已有项目上改动，"只输出改动文件+标注新增/修改"这一条能帮你省最多时间。
- **案例3 的"先列出多种可能原因再验证"**，能避免模型看到报错就立刻"自信地"给一个不一定对的修复方案——这是网页对话里很常见的翻车点。
- 这几个模板里的方括号 `<xxx>` 部分是你每次要填的变量，其余结构建议保持不变，结构稳定了你才能横向比较"这次回复质量是不是变差了"。

