# 工位预订管理系统：代码分析与新人使用指南

> 文档版本：2026-07-13  
> 分析范围：本次上传的 Vue 组件、Composable、Supabase 客户端配置以及 `seats`、`bookings`、`booking_leader_whitelist` 表数据。

---

## 1. 系统概述

本项目是一个基于 **Vue 3 + Supabase** 的多楼层工位预订管理系统，当前支持：

- IFC39、IFC36 两个楼层平面图；
- 固定工位、可预订工位、不可用占位工位；
- 按日期区间查看工位占用状态；
- 组长代员工预订工位；
- 取消、提前释放和自动签到；
- 普通账号、组长白名单、超级管理员三层权限；
- 免登录只读分享链接；
- 工位实时同步；
- CCC 成本中心月度报表、趋势图和 Excel 导出；
- 管理员维护工位类型、固定人员、CCC、邮件开关和组长白名单。

系统采用“前端页面 + Supabase Auth + PostgreSQL 表/RPC + Edge Function 邮件”的结构。前端不仅负责展示，也承担了部分业务校验；真正的权限边界仍必须由 Supabase RLS、数据库约束和 RPC 保证。

---

## 2. 技术结构

### 2.1 前端技术

- Vue 3 Composition API；
- `<script setup>` 单文件组件；
- Supabase JavaScript SDK；
- `@vuepic/vue-datepicker` 日期区间选择器；
- Chart.js 报表趋势图；
- SheetJS `xlsx` 动态导出 Excel；
- Bootstrap 风格的按钮、表单和加载样式；
- 原生 `ResizeObserver`、`localStorage`、Web Crypto API。

### 2.2 环境变量

客户端至少依赖：

```env
VITE_SUPABASE_URL=你的 Supabase Project URL
VITE_SUPABASE_ANON_KEY=你的 Supabase anon/public key
```

缺少任意一项时，`src/lib/supabase.js` 会在应用初始化阶段直接抛错。

### 2.3 依赖的数据库对象

#### 表

| 表名 | 用途 |
|---|---|
| `seats` | 工位主数据、楼层、区域、工位类型、固定人员和 CCC |
| `bookings` | 预订日期、状态、创建人、使用者、CCC、分享令牌 |
| `booking_leader_whitelist` | 允许提交预订的组长邮箱 |
| `system_settings` | 邮件通知和预订规则配置 |
| `holidays` | 法定节假日、调休工作日，用于报表工作日计算 |

#### RPC

前端当前依赖以下数据库函数：

```text
is_booking_leader
release_expired_bookings
check_in_today_bookings
cancel_my_booking
admin_cancel_booking
release_my_booking
admin_release_booking
get_shared_booking_page
```

#### Edge Function

```text
send-reservation-email
```

用于新增、取消和释放预订后的邮件通知。

---

## 3. 页面与代码职责

### 3.1 应用入口

| 文件 | 主要职责 |
|---|---|
| `main.js` | 创建 Vue 应用，引入全局样式和日期组件样式 |
| `App.vue` | 应用总控、登录分流、楼层切换、地图、预订、管理员、报表、分享模式 |
| `style.css` | 全局页面、地图、抽屉、登录、工位状态和响应式样式 |

### 3.2 核心页面组件

| 文件 | 主要职责 |
|---|---|
| `AuthPage.vue` | 登录、注册、忘记密码、重设密码、记住邮箱 |
| `FloorPlan.vue` | IFC39、IFC36 平面图整体布局 |
| `ZoneBlock.vue` | 一个区域内的工位排列、走道和区域标签 |
| `SeatCell.vue` | 单个工位状态、姓名、占用进度和悬停入口 |
| `SeatHoverCalendar.vue` | 工位月历、每天占用情况和有效预订列表 |
| `TimeFilter.vue` | 顶部查看日期区间及查找空闲工位 |
| `BookingQuickModal.vue` | 快捷预订弹框容器 |
| `BookingForm.vue` | 使用者、CCC、日期和使用者邮箱表单 |
| `AvailableSeatFinderModal.vue` | 查询区间内完全空闲的可预订工位 |
| `BookingList.vue` | 右侧预订记录、筛选、分页、取消和释放 |
| `AdminPage.vue` | 超级管理员配置页面 |
| `StatsPage.vue` | CCC 报表、趋势图和 Excel 导出 |
| `ShareView.vue` | 免登录只读分享页面 |
| `ShareDetailCard.vue` | 分享预订详情卡片 |

### 3.3 Composable

| 文件 | 主要职责 |
|---|---|
| `useAuth.js` | Supabase Auth、空闲超时、会话最长时间 |
| `useSeats.js` | 多楼层布局配置、工位读取、虚拟占位、工位统计和实时订阅 |
| `useBookings.js` | 预订 CRUD、冲突检测、状态维护、邮件和实时订阅 |
| `useBookingPermission.js` | 调用 `is_booking_leader()` 获取预订权限 |
| `useAdminSeats.js` | 工位类型、CCC 和组长白名单维护 |
| `useSystemSettings.js` | 邮件通知和预订规则配置 |
| `useStats.js` | 工作日与 CCC 月度折算统计 |
| `useShareToken.js` | 分享链接生成、解析、RPC 验证和数据标准化 |
| `useSeatStatus.js` | 工位状态统一判断 |
| `useAppLifecycle.js` | 登录后加载数据、订阅 Realtime 和销毁资源 |
| `usePlanResize.js` | 地图高度和右侧预订抽屉高度同步 |
| `useUserPrefs.js` | 镜像视图、抽屉状态、分页大小本地保存 |
| `useToast.js` | 全局轻量提示 |
| `useCountUp.js` | 顶部统计数字动画 |

---

## 4. 权限模型

系统当前有三种实际使用角色。

### 4.1 普通登录账号

权限表现：

- 可以登录；
- 可以切换楼层、日期和镜像视图；
- 可以查看工位状态、悬停月历和预订记录；
- 不能提交预订；
- 不能取消或释放预订；
- 顶部显示“只读查看”。

权限校验失败时，系统默认降级为只读，这是合理的安全兜底。

### 4.2 组长白名单账号

邮箱存在于 `booking_leader_whitelist`，或者数据库函数 `is_booking_leader()` 判定为有权限时：

- 可以为员工提交预订；
- 可以取消或提前释放自己创建的预订；
- 不可以管理其他组长创建的预订；
- 不可以进入管理员页面和报表页面。

需要特别区分：

- `created_by_email`：实际提交预订的组长；
- `user_name`：工位实际使用者姓名；
- `reminder_email`：工位实际使用者邮箱。

### 4.3 超级管理员

用户 JWT 的：

```text
app_metadata.app_role = superadmin
```

时，前端识别为超级管理员。

超级管理员具有：

- 组长的全部预订能力；
- 取消、释放任意人的预订；
- 进入管理员页面；
- 修改工位类型和固定人员；
- 修改工位 CCC；
- 配置预订规则和邮件开关；
- 维护组长白名单；
- 查看和导出 CCC 报表。

> 前端的 `isSuperAdmin()` 只控制页面显示。数据库必须使用 RLS、RPC 或服务端校验再次确认超级管理员权限，不能把前端判断当作安全边界。

### 4.4 分享访问者

带 `?share=...` 的链接会优先进入分享模式，不要求登录。

分享访问者只能：

- 查看目标预订；
- 查看脱敏后的平面图和区间占用；
- 查看目标工位位置；
- 不能预订、取消、释放或进入后台。

匿名页面通过 `get_shared_booking_page` RPC 获取数据，不直接读取 `seats` 和 `bookings` 表，这一设计方向是正确的。

---

## 5. 工位状态规则

### 5.1 数据状态

| 条件 | 页面状态 |
|---|---|
| `is_virtual_placeholder = true` | 不可用 |
| `is_active = false` | 不可用 |
| `seat_type = fixed` | 固定工位 |
| `seat_type = flexible` 且当前查询区间存在预订 | 已订或部分占用 |
| `seat_type = flexible` 且当前查询区间无预订 | 空闲 |

### 5.2 部分占用

“部分占用”不是数据库状态，也不是 `useSeatStatus()` 返回值，而是 `SeatCell.vue` 根据当前显示月份中的占用天数计算出的展示状态。

例如：

- 当前月 31 天；
- 某工位在该月占用 10 天；
- 工位格显示约 `10 / 31` 的占用进度；
- 用户仍可点击预订，最终是否冲突由提交时的区间校验决定。

### 5.3 靠窗工位特殊规则

IFC39 的 `窗-W` 工位如果没有 `fixed_owner`，前端会将其转成不可用虚拟占位格，而不是可预订工位。

### 5.4 楼层布局

#### IFC39

- 靠窗 W 区；
- 左右 A～E 外围区域；
- F 区和安全屋区域；
- G1～G5 内部区域；
- 部分位置通过前端配置禁用并显示为空白占位。

#### IFC36

- 沿用相同建筑骨架；
- 不显示靠窗区；
- F 区按普通工位网格展示；
- 当前真实数据主要位于“左-*”区域；
- 未配置位置由前端虚拟占位补齐。

---

## 6. 预订业务规则

### 6.1 日期规则

- 采用“开始日期～结束日期”的全天日期跨度；
- 不区分具体小时和分钟；
- 不能预订今天之前的日期；
- 开始日期不能晚于结束日期；
- 同一工位只要日期区间发生交集，就视为冲突；
- 当天开始的预订会直接创建为 `checked_in`；
- 未来开始的预订创建为 `confirmed`。

### 6.2 同邮箱重复预订

默认规则：

```text
prevent_same_email_overlap = true
```

即同一个 `reminder_email` 在相同或交叉日期内不能预订多个工位。

超级管理员可以在后台关闭此规则。

### 6.3 工位冲突

提交前前端查询：

```text
status in ('confirmed', 'checked_in')
start_date <= 新结束日期
end_date >= 新开始日期
```

此外，代码会处理 PostgreSQL `23P01` 错误，说明数据库预计还存在日期区间排他约束，用于应对多人同时抢订同一工位。

### 6.4 状态变化

| 操作 | 结果 |
|---|---|
| 新增今天开始的预订 | `checked_in` |
| 新增未来预订 | `confirmed` |
| 用户取消 | `cancelled` |
| 提前释放 | `released` |
| 结束日期已过 | 调用 RPC 自动释放 |
| 日期到达当天 | 调用 RPC 自动签到 |

### 6.5 取消和释放区别

- **取消**：预订计划作废，一般适用于尚未使用或不再需要的记录；
- **提前释放**：工位已经处于使用中或原计划仍有效，但现在提前交还；
- 两者都会从当前可用性占用计算中移除；
- 历史记录仍保留在 `bookings` 表中。

### 6.6 邮件通知

新增、取消和释放后，系统会尝试向以下邮箱发送通知：

1. `created_by_email`；
2. `reminder_email`；
3. 当前操作人邮箱。

系统使用 `Set` 去重，同一个邮箱不会重复发送。

邮件失败不会回滚已经成功的预订、取消或释放操作；页面会提示“业务成功，但邮件失败”。

---

## 7. 分享链接逻辑

新增预订时：

1. 浏览器生成 6 位随机 `nonce`；
2. `nonce` 写入 `bookings.share_token`；
3. 分享 URL 包含：`seatId`、`bookingId`、日期、`nonce` 和 `exp`；
4. URL 通过邮件发送；
5. 打开时先在客户端解析；
6. 再调用 `get_shared_booking_page` RPC，使用 `bookingId + nonce` 做数据库校验；
7. RPC 返回脱敏工位、目标预订和占用数据。

代码设计的预期有效期为：

```text
预订结束日 + 30 天
```

关于当前实现中的安全风险，见本文“代码扫描问题”章节。

---

## 8. CCC 报表逻辑

### 8.1 CCC 来源

| 工位类型 | CCC 来源 |
|---|---|
| 固定工位 | `seats.cost_center` |
| 可预订工位 | 每条 `bookings.cost_center` |

因此固定工位切换为可预订后，后续报表不再按工位主表中的 CCC 计算，而是按每次预订填写的 CCC 计算。

### 8.2 固定工位贡献

一个固定工位在某个月的贡献：

```text
固定工位天数 = 查询区间内该月有效工作日数
```

例如某月有 21 个工作日，一张固定工位贡献 21 工位·天。

### 8.3 可预订工位贡献

每条 flexible 预订会：

1. 拆分成每天；
2. 排除周末和法定节假日；
3. 包含调休补班日；
4. 按月份和预订 CCC 累加。

### 8.4 折算工位数

```text
折算工位数 = 使用总天数 ÷ 当月有效工作日数
```

例如：

```text
某 CCC 在 7 月使用 42 工位·天
7 月有 21 个有效工作日
折算工位数 = 42 ÷ 21 = 2.0
```

### 8.5 当前页面“使用率”的实际含义

当前代码使用：

```text
某 CCC 使用率 = 某 CCC 汇总折算工位数 ÷ 所有 CCC 汇总折算工位数
```

它表达的是 **CCC 在全部已统计工位使用中的占比**，并不是通常意义上的“工位利用率”。

通常意义的利用率更接近：

```text
实际使用工位·天 ÷ 可提供工位·天
```

建议将当前列名改为“CCC 使用占比”，或者另行补充真正的利用率指标。

### 8.6 报表功能

超级管理员可以：

- 选择日期区间；
- 按 CCC 筛选；
- 查看月度折算工位数；
- 查看使用总天数；
- 查看当前代码定义的使用占比；
- 查看当年 1～12 月趋势图；
- 隐藏或显示某条 CCC 曲线；
- 导出 `CCC月度透视` Excel 工作表。

---

## 9. 当前 CSV 数据概览

### 9.1 工位数据

总计：**459 条**。

| 指标 | 数量 |
|---|---:|
| IFC39 | 285 |
| IFC36 | 174 |
| 活跃工位 | 444 |
| 非活跃工位 | 15 |
| 固定工位 | 375 |
| 可预订工位 | 83 |
| disabled | 1 |

`is_virtual_placeholder` 在 CSV 中全部为 `false`。当前页面上的大量灰色空位主要是前端运行时生成的虚拟占位，不是数据库真实行。

### 9.2 工位 CCC

| CCC | 工位数 |
|---|---:|
| CWA1 | 228 |
| CXS5 | 1 |
| 空值 | 230 |

### 9.3 预订数据

总计：**107 条**。

| 状态 | 数量 |
|---|---:|
| cancelled | 44 |
| released | 37 |
| checked_in | 19 |
| confirmed | 7 |

楼层分布：

| 楼层 | 预订数 |
|---|---:|
| IFC39 | 104 |
| IFC36 | 3 |

预订 CCC 包含：

```text
CWA1、C01S、CXS5、C01R、C01M
```

### 9.4 白名单

当前 CSV 有 3 个组长邮箱：

```text
june.zhuang@pccw.com
kenneth.lau@pccw.com
robbin.lb.zhang@pccw.com
```

### 9.5 数据质量提示

当前数据中值得关注：

- 17 条历史预订没有 `reminder_email`；
- IFC36 有 169 个固定工位没有 `fixed_owner`，也没有 `cost_center`；
- IFC39 有 23 个固定工位没有 `cost_center`；
- 当前有效预订中存在大量同一 `reminder_email` 同期占用多个工位的历史数据；
- 同一工位的有效预订没有发现日期重叠；
- 未发现孤立的 `seat_id`；
- 未发现楼层内重复工位编号或重复 `block_code + local_index`。

这些历史数据可能早于“同邮箱禁止重复预订”规则，也可能是组长暂时复用了同一个提醒邮箱。启用该规则后，新预订会被前端拦截，但历史数据不会自动清理。

---

# 第二部分：新人使用说明

## 10. 第一次登录

### 10.1 登录

1. 打开系统网址；
2. 输入邮箱和密码；
3. 可勾选“记住邮箱”；
4. 点击“登录”。

“记住邮箱”只把邮箱保存到当前浏览器 `localStorage`，不会保存密码。

### 10.2 注册

页面提示仅允许 `@pccw.com` 公司邮箱注册。

1. 切换到“注册”；
2. 输入公司邮箱；
3. 设置至少 6 位密码；
4. 再次确认密码；
5. 点击“注册并登录”。

> 当前前端代码没有真正校验 `@pccw.com` 后缀，必须由 Supabase Auth Hook、数据库或服务端补充强制限制。不能只依赖页面提示。

### 10.3 忘记密码

1. 点击“忘记密码”；
2. 输入注册邮箱；
3. 点击发送；
4. 打开邮件中的链接；
5. 在 `/reset-password` 页面设置新密码；
6. 完成后重新登录。

部署平台必须把 `/reset-password` 配置为可回退到前端入口，否则刷新该地址时可能出现 404。

### 10.4 会话超时

- 连续 30 分钟无操作会自动退出；
- 退出前 5 分钟显示倒计时；
- 点击“继续使用”可以续期空闲时间；
- 单次登录最长 8 小时；
- 当天 23:59:59 也会强制结束，会取两者中更早的时间。

---

## 11. 认识主页面

### 11.1 顶部区域

从左到右包括：

- 系统名称；
- 楼层选择：IFC39 / IFC36；
- 查看日期区间；
- 查找空闲工位；
- 总工位、固定、已订、空闲数量；
- Realtime 实时同步状态；
- 当前登录邮箱；
- “可预订”或“只读查看”权限标识；
- 退出登录；
- 超级管理员入口；
- 报表入口；
- 镜像视图切换。

### 11.2 地图状态

不同工位格用于表达：

- 固定工位；
- 当前区间完全占用；
- 当前月份部分占用；
- 空闲可预订；
- 不可用或虚拟占位。

将鼠标停在工位上，可以查看该工位月历和有效预订列表。

### 11.3 镜像视图

点击“镜像视图”后，整张地图会翻转，工位文字会再次反向纠正，便于按照不同方向查看办公室。

镜像选择会保存在当前浏览器，下次打开仍会沿用。

### 11.4 预订记录抽屉

页面右侧有“预订记录”舌形按钮：

- 点击展开或收起；
- 可以按工位、状态和使用者筛选；
- 可以调整每页数量；
- 组长只能操作自己创建的记录；
- 超级管理员可以操作全部记录。

---

## 12. 查看某个日期区间

1. 在顶部选择开始日期和结束日期；
2. 点击查询；
3. 地图重新加载与区间相交的有效预订；
4. 顶部“已订”和“空闲”数字随之变化；
5. 工位格的占用状态和悬停信息同步更新。

默认查询区间为：

```text
今天 ～ 一个月后的同一天
```

如果下个月没有同一天，例如 1 月 31 日，则使用目标月份最后一天。

---

## 13. 提交预订

只有顶部显示“可预订”的账号可以提交。

### 方法一：直接在地图选工位

1. 先设置顶部日期区间；
2. 点击一个可预订工位；
3. 系统打开快捷预订弹框；
4. 填写实际使用者姓名；
5. 填写或选择 CCC；
6. 确认预订日期；
7. 填写实际使用者邮箱；
8. 点击“提交预订”。

### 方法二：查找空闲工位

1. 设置顶部日期区间；
2. 点击“查找空闲工位”；
3. 系统重新读取最新预订；
4. 只展示整个日期区间都没有冲突的 flexible 工位；
5. 可输入工位编号过滤；
6. 点击一个工位；
7. 继续填写预订表单。

### 填写要求

| 字段 | 要求 |
|---|---|
| 使用者 | 必填，填写实际坐在该工位的人 |
| CCC | 必填，提交前自动转成大写 |
| 日期 | 必填，不能早于今天 |
| 使用者邮箱 | 必填，需符合基本邮箱格式 |
| 创建人 | 自动使用当前登录邮箱 |

建议 CCC 使用统一选项：

```text
CWA1、CXS5、C01R、C01M、C01S
```

当前输入框允许自由输入，因此填写错别字会直接产生新的报表分类。

### 成功后

- 预订写入数据库；
- 地图和记录重新加载；
- 今天开始的记录显示“使用中”；
- 未来记录显示“已确认”；
- 邮件异步发送给预订者和使用者；
- 邮件中包含免登录只读分享链接。

---

## 14. 取消预订

1. 展开右侧“预订记录”；
2. 找到目标记录；
3. 点击“取消”；
4. 核对工位、使用者和日期；
5. 确认操作。

普通组长只能取消自己创建的预订。超级管理员可以取消任意记录。

取消成功后：

- 状态变成 `cancelled`；
- 工位不再被该记录占用；
- 历史记录继续保留；
- 系统尝试发送取消通知。

---

## 15. 提前释放工位

1. 展开预订记录；
2. 找到仍有效的预订；
3. 点击“释放”；
4. 确认操作。

释放成功后：

- 状态变成 `released`；
- 工位立即可被其他符合条件的日期预订；
- 系统尝试发送释放通知。

---

## 16. 打开分享链接

邮件中的分享链接不需要登录。

打开后可以查看：

- 目标楼层；
- 目标工位；
- 使用者；
- 预订日期；
- 预订状态；
- 同期脱敏工位地图。

如果出现以下提示：

```text
链接已失效或格式有误
```

可能原因：

- URL 被截断；
- 查询参数被修改；
- 分享 nonce 已失效；
- 预订记录不存在；
- 令牌超过有效期；
- `get_shared_booking_page` RPC 没有正确部署或授权 anon 调用。

---

## 17. 超级管理员操作指南

### 17.1 进入后台

登录后顶部出现“管理员”按钮时，说明 JWT 中存在 `app_role=superadmin`。

点击后进入当前楼层的管理页面。

### 17.2 将工位设为固定工位

1. 选择工位；
2. 选择“设为固定工位”；
3. 输入固定人员姓名；
4. 查看等效 SQL 预览；
5. 点击确认修改。

当前操作只更新：

```text
seat_type = fixed
fixed_owner = 姓名
```

不会自动设置 CCC，应继续到“工位成本中心”区域检查并保存。

### 17.3 将工位设为可预订

1. 选择工位；
2. 选择“设为可预订”；
3. 确认修改。

系统更新：

```text
seat_type = flexible
fixed_owner = null
```

`cost_center` 当前不会被清空。报表对 flexible 工位使用 `bookings.cost_center`，因此保留的工位 CCC 一般不会参与 flexible 统计，但会增加数据理解成本。

### 17.4 修改工位 CCC

1. 选择工位；
2. 输入 CCC；
3. 系统自动转大写；
4. 确认保存。

主要用于固定工位。可预订工位的 CCC 应在每次预订时填写。

### 17.5 配置重复预订规则

“同邮箱同时间段禁止重复预订”默认开启。

建议保持开启，除非业务明确允许一个邮箱代表多人批量占多个工位。

### 17.6 配置邮件通知

可以分别控制：

- 新增预订；
- 取消预订；
- 提前释放。

关闭后只停止后续邮件，不会影响数据库中的业务操作。

### 17.7 维护组长白名单

新增：

1. 输入 `@pccw.com` 邮箱；
2. 点击添加；
3. 重复邮箱会提示已存在。

删除：

1. 在列表找到邮箱；
2. 点击删除；
3. 删除后该账号下次权限刷新时变成只读。

当前页面在白名单被后台删除后，会关闭已打开的预订弹框；提交时也会再次调用 RPC 校验，避免只依赖登录时缓存。

### 17.8 操作日志

管理员页面底部日志只保存在本次浏览器会话的 Vue 内存中：

- 刷新页面后消失；
- 不是正式审计日志；
- 数据库应另建审计表或触发器保存关键配置变化。

---

## 18. 报表使用指南

1. 使用超级管理员账号登录；
2. 点击顶部“报表”；
3. 选择统计日期范围；
4. 系统加载 `bookings`、`seats` 和 `holidays`；
5. 可选择某个 CCC；
6. 查看月度折算工位、工位·天和使用占比；
7. 查看当前年度趋势曲线；
8. 点击导出 Excel。

### 报表中没有某个 CCC

检查：

- 固定工位是否填写 `seats.cost_center`；
- flexible 预订是否填写 `bookings.cost_center`；
- 预订状态是否为 `cancelled`；
- 日期是否与查询区间相交；
- 楼层是否正确；
- `holidays` 是否配置错误。

---

## 19. 常见问题

### 19.1 顶部显示“只读查看”

可能原因：

- 邮箱不在组长白名单；
- 白名单邮箱大小写或空格异常；
- `is_booking_leader()` RPC 报错；
- 登录会话已经变化；
- RLS 或函数权限配置异常。

### 19.2 地图显示不出工位

检查：

- `.env`；
- `seats` 表是否有对应 `floor_id`；
- `is_active` 是否为 `true`；
- RLS 是否允许登录用户读取；
- `block_code` 和 `local_index` 是否匹配前端布局；
- IFC39 靠窗工位是否缺少 `fixed_owner`。

### 19.3 预订成功但没有邮件

业务写库和邮件发送是分开的。

检查：

- 管理员是否关闭对应邮件开关；
- `send-reservation-email` 是否部署；
- Edge Function CORS；
- Function 日志；
- 邮件服务域名验证；
- 使用者邮箱是否填写；
- 垃圾邮件箱。

### 19.4 预订记录不完整

右侧列表当前只加载：

- `end_date >= 今天往前 60 天`；
- 最多 200 条；
- 当前楼层。

更老的记录或超过 200 条的数据不会展示。

### 19.5 固定工位在报表中没有统计

顶部统计只要 `seat_type=fixed` 就会计入固定工位；CCC 报表还要求 `cost_center` 非空。

因此可能出现：

```text
顶部显示固定工位，但 CCC 报表没有该工位
```

---

# 第三部分：代码扫描问题与优化建议

## 20. P0 / P1：建议优先修复

### 20.1 注册域名只提示、未实际校验

**现状**

登录页提示“仅限 @pccw.com”，但 `AuthPage.validateForm()` 和 `useAuth.signUp()` 都没有域名检查。

**风险**

任意格式正确的外部邮箱都可以调用 Supabase 注册，除非数据库或 Auth Hook 已经额外限制。

**建议**

同时在三层限制：

1. 前端即时校验；
2. Supabase Auth Hook 或服务端注册接口；
3. 数据库用户初始化逻辑再次校验。

仅修前端不足以防止绕过。

---

### 20.2 分享链接过期时间可以被客户端篡改

**现状**

分享 token 只是 Base64 JSON，不是签名令牌。`exp` 只在客户端 `parseShareToken()` 中判断，调用 RPC 时只发送：

```text
bookingId + nonce
```

**风险**

持有有效 `bookingId + nonce` 的人可以修改 Base64 中的 `exp`，让客户端继续发起 RPC。除非 `get_shared_booking_page` 在数据库内根据 `bookings.end_date + 30 天` 独立校验，否则链接的“结束日 + 30 天”并不可靠。

**建议**

- RPC 内强制校验有效期，不信任前端 `exp`；
- 或改成服务端签名 token；
- 使用不可预测的 128 位以上随机 token；
- 支持管理员主动吊销或轮换分享 token。

---

### 20.3 CCC 报表月末日期在 UTC+8 会少一天

**现状**

代码多处使用：

```js
new Date(year, month, 0).toISOString().slice(0, 10)
```

在 Asia/Taipei / Asia/Shanghai 时区，例如 2026 年 7 月最后一天会得到：

```text
本地：2026-07-31 00:00
转 UTC：2026-07-30 16:00
截取结果：2026-07-30
```

**影响**

- 月末少算一天；
- 工作日数可能少 1；
- flexible 工位·天和折算工位数可能错误；
- 趋势图已过月份/未来月份边界可能错误；
- “上月”快捷区间结束日可能错误。

**建议**

统一使用本地日期格式函数：

```js
const last = new Date(year, month, 0)
return toDateInput(last)
```

不要用 `toISOString().slice(0, 10)` 格式化本地业务日期。

---

### 20.4 工位类型切换没有检查有效预订

**现状**

管理员可以直接把 flexible 工位改成 fixed，也可以把 fixed 改成 flexible。更新前没有查询该工位是否存在 `confirmed` 或 `checked_in` 预订。

**风险**

- 工位仍有有效预订，却在地图中变成固定工位；
- 有效预订记录继续存在；
- 顶部状态、预订记录和报表口径冲突；
- 使用者可能无法理解工位为何突然不可预订。

**建议**

使用数据库事务 RPC：

1. 锁定工位；
2. 查询有效预订；
3. 有冲突时拒绝切换，或要求先取消/释放；
4. 更新类型、固定人员和 CCC；
5. 写审计日志。

---

### 20.5 同邮箱冲突只靠前端查询，存在并发竞态

**现状**

同一使用者邮箱的重复预订由前端先查再插入。两个请求同时执行时，都可能先查到“无冲突”，然后分别插入不同工位。

**当前数据表现**

有效预订中已经存在大量同邮箱同时占用多个工位的记录，虽然其中部分可能是历史批量操作或规则启用前数据。

**建议**

把“同邮箱同区间不可重叠”放入数据库 RPC 或约束中原子执行。配置关闭时再由数据库函数决定是否跳过。

---

### 20.6 前端权限不是安全边界

`canBook`、`isSuperAdminUser` 和按钮禁用只影响页面交互。攻击者可以直接调用 Supabase API。

必须确认：

- `bookings` INSERT 只允许 `is_booking_leader()`；
- 普通组长只能取消/释放 `created_by_user_id = auth.uid()` 的记录；
- 超级管理员 RPC 再次检查 JWT role；
- `seats`、`system_settings`、白名单的写入只允许超级管理员；
- anon 只能执行严格脱敏的分享 RPC。

本次未上传 SQL，无法确认这些数据库保护是否完整。

---

## 21. P1 / P2：业务一致性问题

### 21.1 IFC36 固定工位数据不完整

CSV 中 IFC36：

- 174 个工位全部 active；
- 169 个标记为 fixed；
- 169 个 fixed 都没有 `fixed_owner`；
- 169 个 fixed 都没有 `cost_center`。

结果：

- 顶部会把它们统计为固定工位；
- 工位格固定人员显示“暂未录入”；
- CCC 报表完全不统计这些 fixed 工位；
- 与“真实固定人员工位”的语义混在一起。

建议明确数据状态：

- 未启用位置应 `is_active=false` 或 `seat_type=disabled`；
- 未分配但可用位置应设为 flexible；
- 真正 fixed 必须要求 `fixed_owner` 和 `cost_center`。

### 21.2 IFC39 有 23 个固定工位缺少 CCC

这些工位会进入顶部固定数量，但不会进入 CCC 报表。

建议增加数据库约束或管理员保存校验：

```text
seat_type = fixed 时，fixed_owner 和 cost_center 必填
```

### 21.3 “使用率”命名与计算口径不符

当前计算的是 CCC 占全部折算工位的比例，不是工位利用率。

建议改名：

```text
CCC 使用占比
```

若需要真实利用率，应引入：

- 可预订工位总数；
- 每月有效工作日；
- 可提供工位·天；
- 实际被预订工位·天。

### 21.4 `loadBookings()` 默认吞掉错误

`loadBookings()` 捕获异常后只写控制台，默认不抛出，也没有独立 `bookingsError`。

风险：

- 页面可能继续显示旧数据；
- 或显示“全部空闲”；
- 用户误以为查询成功；
- `loadAppData()` 的 `Promise.all` 可能仍然成功完成。

建议：

- 增加 `bookingsLoading`、`bookingsError`；
- 首次加载失败必须抛出；
- 手动查询失败保留旧数据并明确提示；
- 不要把网络失败解释为空数据。

### 21.5 自动签到和自动释放依赖有人打开页面

当前通过页面加载和午夜定时器调用 RPC。

如果无人打开系统：

- 状态不会准点自动签到；
- 过期状态不会准点释放；
- 直到下一位用户加载页面才修正。

建议使用 Supabase Cron / pg_cron 或服务端定时任务维护状态，浏览器调用只作为补偿。

### 21.6 初次加载重复调用过期释放 RPC

`loadSeats()`、`loadBookings()`、`loadAllBookings()` 并行时，后两者都会调用 `release_expired_bookings`；随后 `runAutoCheckIn()` 又调用一次。

建议在应用刷新周期内只调用一次状态维护 RPC，然后并行读取 seats 和 bookings。

### 21.7 预订记录存在静默截断

`loadAllBookings()`：

```text
只取最近 60 天结束的记录
最多 200 条
```

页面没有提示“当前仅展示部分记录”。

建议：

- 使用服务端分页；
- 显示总数；
- 明确时间范围；
- 报表和审计不要依赖右侧列表数据。

### 21.8 CCC 选项重复定义

`BookingForm.vue` 和 `AdminPage.vue` 各自维护同一数组。未来新增 CCC 容易漏改。

建议建立：

- `cost_centers` 数据表；或
- 单一 `constants/costCenters.js`；
- 更理想的是管理员可维护的 CCC 主数据。

### 21.9 CCC 允许自由输入，容易产生脏分类

当前 datalist 只是建议，用户仍可输入任何字符串。

例如：

```text
CWA1
CWAI
CWA-1
```

会被报表视为三个成本中心。

建议使用受控下拉，或建立 CCC 主表并使用外键。

### 21.10 历史预订缺少使用者邮箱

17 条记录的 `reminder_email` 为空。取消/释放时无法通知实际使用者，只能通知创建人或操作人。

建议：

- 补录历史数据；
- 数据库设 `reminder_email NOT NULL`；
- RPC/INSERT 层强制邮箱格式；
- 不依赖前端验证。

### 21.11 分享 nonce 长度偏短

当前字符集约 56 个字符、长度 6，理论空间约 300 亿，约 35 位熵。

虽然同时需要 `bookingId`，但长期公开链接建议使用：

```text
crypto.randomUUID()
```

或至少 16～32 字节随机数，并在数据库中只保存哈希。

### 21.12 URL 中的分享 token 可能进入日志和浏览器历史

查询参数可能出现在：

- 浏览器历史；
- 代理日志；
- Web 分析工具；
- Referer；
- 截图和复制记录。

建议：

- 设置严格 Referrer-Policy；
- RPC 验证成功后用 `history.replaceState` 清理地址栏；
- 支持吊销；
- 避免加载第三方资源；
- 对分享页面设置较短有效期。

---

## 22. P2 / P3：代码质量与维护性

### 22.1 `useSeatStatus()` 有重复判断

```js
if (seat?.is_virtual_placeholder || seat?.is_active === false)
if (seat?.is_active === false)
```

第二个分支永远是重复逻辑，可以删除。

### 22.2 大组件职责过重

- `App.vue` 超过 1100 行；
- `StatsPage.vue` 超过 1100 行；
- `FloorPlan.vue` 超过 700 行；
- `AdminPage.vue` 超过 700 行。

建议拆分：

```text
AppHeader
RecordsDrawer
SessionWarningModal
AdminSeatTypeCard
AdminCostCenterCard
AdminBookingRulesCard
AdminEmailSettingsCard
AdminWhitelistCard
CCCReportTable
CCCTrendChart
```

### 22.3 分享数据标准化代码过长

`useShareToken.js` 接近 900 行，包含大量兼容和推断逻辑，说明 RPC 返回结构不够稳定。

建议在数据库 RPC 中返回固定 JSON Schema，前端只做最小字段校验，减少根据编号推断区域和类型的逻辑。

### 22.4 Realtime 回调没有楼层过滤

任意楼层工位或预订变化都会触发当前页面刷新。

建议：

- Realtime 按 `floor_id` 过滤；
- 或在 payload 中判断关联楼层；
- bookings 高频事件增加 debounce；
- 避免一次批量操作触发多次全量查询。

### 22.5 全局单例状态需要明确

`useAuth.js` 和 `useSystemSettings.js` 使用模块级 `ref`，多个组件调用时共享同一状态。这本身可行，但应明确标注为 singleton store，或者改为 Pinia，降低新人误解。

### 22.6 原生 `confirm` / `alert` 体验不一致

项目多数反馈使用 Toast，但取消、释放、管理员操作和导出失败仍使用原生弹框。

建议统一为应用 Modal / Confirm 组件，提高样式一致性和可测试性。

### 22.7 缺少统一错误分类

当前错误消息来自：

- Supabase 原始错误；
- RPC 自定义异常；
- 前端中文提示；
- Edge Function 文本响应。

建议定义错误码，例如：

```text
BOOKING_CONFLICT
EMAIL_ALREADY_BOOKED
BOOKING_PERMISSION_DENIED
SEAT_NOT_ACTIVE
SHARE_TOKEN_EXPIRED
```

前端按错误码显示稳定文案。

---

## 23. 推荐修复顺序

### 第一批：安全和统计正确性

1. 后端强制 `@pccw.com` 注册限制；
2. 分享 RPC 独立校验过期时间；
3. 修复所有 `toISOString().slice(0, 10)` 本地日期问题；
4. 工位类型切换改为事务 RPC；
5. 同邮箱重叠规则放入数据库原子校验；
6. 全面复核 RLS 和 RPC 权限。

### 第二批：数据治理

1. 清理 IFC36 未分配 fixed 数据；
2. 补齐固定工位 `fixed_owner` 和 `cost_center`；
3. 补齐历史 `reminder_email`；
4. 建立 CCC 主数据；
5. 增加数据库约束和审计日志。

### 第三批：可靠性和体验

1. bookings 独立 loading/error；
2. 自动签到/释放迁移到服务端定时任务；
3. 右侧记录改服务端分页；
4. Realtime 增加楼层过滤和防抖；
5. 统一确认弹框、错误码和 Toast。

### 第四批：代码重构

1. 拆分 App、Admin、Stats、FloorPlan；
2. 抽取 CCC 常量或主数据；
3. 固化分享 RPC 返回 Schema；
4. 使用 Pinia 管理全局状态；
5. 增加单元测试和端到端测试。

---

## 24. 建议测试清单

### 登录与权限

- 普通账号只读；
- 白名单组长可预订；
- 删除白名单后立即失去权限；
- 超级管理员可进入后台；
- 外部邮箱无法注册；
- 30 分钟空闲退出；
- 8 小时或当天结束退出。

### 预订

- 同工位不重叠；
- 边界日期相同也算冲突；
- 同邮箱不同工位重叠被拒；
- 两个浏览器同时抢订；
- 今天预订为 checked_in；
- 未来预订为 confirmed；
- 不能预订过去日期；
- 取消和释放权限正确。

### 管理员

- 有有效预订的 flexible 不能直接改 fixed；
- fixed 必须有 owner 和 CCC；
- 白名单只能添加公司邮箱；
- 普通用户无法直接写 seats/system_settings/whitelist；
- 操作有数据库审计记录。

### 报表

- 月末日期不缺一天；
- 周末正确排除；
- 法定假日正确排除；
- 调休周末正确计入；
- 跨月预订正确拆分；
- fixed 与 flexible CCC 口径正确；
- 无 CCC 数据明确提示；
- Excel 与页面一致。

### 分享

- 正常链接可访问；
- 修改 bookingId 无法访问；
- 修改 nonce 无法访问；
- 修改 exp 不能延长有效期；
- 取消/释放后的分享规则符合业务预期；
- 分享页不泄露创建人 ID、邮箱和其他敏感字段。

---

## 25. 总结

当前项目已经形成较完整的业务闭环：

```text
登录 → 权限识别 → 楼层地图 → 日期查询 → 工位预订
→ 邮件分享 → 取消/释放 → 管理配置 → CCC 报表
```

代码中值得肯定的部分包括：

- 登录、只读、组长和超级管理员分层清晰；
- 预订提交时会再次校验白名单；
- 同工位冲突同时考虑前端查询和数据库排他错误；
- 分享页不直接开放匿名表读取；
- fixed 与 flexible 的 CCC 来源已经区分；
- Realtime、自动状态维护、邮件失败不回滚业务等流程较完整；
- 工位地图、部分占用、悬停月历和镜像视图体验较丰富；
- 管理员返回地图后的抽屉高度重新绑定逻辑已经得到修复。

上线前最需要解决的是：

1. 注册域名后端限制；
2. 分享链接有效期的服务端校验；
3. UTC+8 月末日期错误；
4. 工位类型切换与有效预订的一致性；
5. 同邮箱并发冲突的数据库保护；
6. IFC36 和固定工位 CCC 数据治理；
7. 完整复核 Supabase RLS、RPC 和匿名分享权限。

完成这些后，系统会从“功能完整的内部工具”进一步接近“数据口径稳定、权限可靠、可长期维护的企业应用”。
